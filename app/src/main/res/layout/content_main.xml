<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/content_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.apps.bluetoothlowenergy_basics.MainActivity"
    tools:showIn="@layout/activity_main">

    <ToggleButton
        android:layout_width="match_parent"
        android:layout_height="30dp"
        android:text="Scan"
        android:background="@color/colorAccent"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:textColor="#FFF"
        android:textSize="15dp"
        android:id="@+id/prelollipopToggleButton"
        android:layout_gravity="center_horizontal"
        android:textOn="Scanning..."
        android:layout_margin="5dp"
        android:textOff="Pre-Lollipop Scan" />


    <ToggleButton
        android:layout_margin="5dp"
        android:layout_below="@id/prelollipopToggleButton"
        android:layout_width="match_parent"
        android:layout_height="30dp"
        android:text="Scan"
        android:background="@color/colorAccent"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:textColor="#FFF"
        android:textSize="15dp"
        android:textOn="Scanning..."
        android:id="@+id/postlollipopToggleButton"
        android:layout_gravity="center_horizontal"
        android:textOff="Lollipop and  After Lollipop Scan" />
'

    <ListView
        android:layout_below="@id/postlollipopToggleButton"
        android:id="@android:id/list"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

</RelativeLayout>
